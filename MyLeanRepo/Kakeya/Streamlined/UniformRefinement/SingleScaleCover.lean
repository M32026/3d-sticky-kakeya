import MyLeanRepo.Kakeya.Streamlined.DilatedTubeCover

/-!
# Compatibility import for paper-facing single-scale dilated covers

The canonical definitions now live in
`MyLeanRepo.Kakeya.Streamlined.DilatedTubeCover`.  The historical strict
`CloseTubeCover` construction input is intentionally not re-exported here;
its compatibility module is `SingleScaleCloseTubeCover`.
-/
